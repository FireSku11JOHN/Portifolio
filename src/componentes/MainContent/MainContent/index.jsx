export const MainContent = ({ children }) => {
    return (
        <main className="bg-bg-main-content px-16 py-16 rounded-t-3xl
            text-ligth MainContent relative z-10">
            {children}
        </main>
    )
}